# Skills-only starter

Load the skills in `.agents/skills/` when they match the task:

- `test-plan` — what to verify after a change
- `changelog-notes` — user-facing changelog bullets
- `issue-triage` — reproduce, severity, next step

Keep answers short. Do not invent credentials or put secrets in files.
