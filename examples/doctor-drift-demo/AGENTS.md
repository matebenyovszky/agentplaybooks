# Doctor drift demo

This tree is a teaching fixture. Instructions and the portable skill are
canonical. Cursor and Claude copies of the same skill are missing a bundled
reference file on purpose so `doctor` reports `skill.drift`.

Do not add credentials here. Use environment references such as
`${EXAMPLE_MCP_TOKEN}` in MCP configs if you add any.
